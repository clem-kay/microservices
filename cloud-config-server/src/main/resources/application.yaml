server:
  port: 9000

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/clem-kay/SpringBootMicrosServices/tree/main
          cloud-on-start: true